
name: cleanshift
description: CleanShift mobile app for clients and cleaners.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=2.17.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^2.4.1
  firebase_auth: ^4.2.5
  cloud_firestore: ^4.2.0
  provider: ^6.0.5

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/icons/
